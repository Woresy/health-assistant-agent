# 更新日志

## 2026-09-16

### 餐食图片识别

- 上传餐食图片后自动识别并预填食物名称。识别只负责预填，候选确认、数据检索、营养计算和确认保存四步一步未变。
- 提供两种可替换的识别后端：多模态模型能识别米饭、番茄炒蛋等中餐菜品；本地 YOLOv8n 离线推理不联网也不产生费用，但只认得苹果、香蕉、橙、西兰花、胡萝卜、披萨、三明治、热狗、甜甜圈和蛋糕这 10 类，中餐会被识别成错误的西式食物，界面对此直接说明。
- 默认关闭。未开启、缺少权重、模型调用失败或识别不到时，都会说明缺什么、怎么补、在那之前还能做什么，并保持手动填写完整可用。
- 份量不做预填：识别给不出重量，不用检测框面积编造数字。
- 使用多模态模型时图片会发送给所配置的服务商，本机仍不保存原图；本地模式全程离线。两者的差别在数据与隐私说明中列出。
- Agent 新增第 16 个受控工具 `detect_food`，沿用同一套结果协议、超时预算和确认边界。
- 识别证据单独记录，只保留图片摘要和字节数，不记录图片内容、文件名或路径。

### 饮食记录流程

- 去掉「匹配食物」和「查看营养估算」两个按钮：食物名称、候选或份量任意一项变化都会自动重新匹配并试算。上传之后的点击次数从 4 次降到 2 次。
- 食物候选由下拉框改为平铺单选，一眼可见、点一下即可；新增常用份量快捷按钮，替用户省去打字。
- 检索精确命中标准名或人工维护的别名时，界面预选对应候选；用的是哪条食物数据全程显示、可一键更换，仍然需要点「保存这一餐」才会写入。
- 份量未填时用中文说明还差哪一格，不再出现组件层的英文校验提示；候选和份量都缺时一次说完，不再点一次补一项。
- 界面文案改为日常说法：「吃的是什么」「吃了多少（克）」「保存这一餐」。营养预览先给「鸡蛋 100 克 大约 139 千卡」这样的结论，再列蛋白质、脂肪和碳水明细。

### 食物数据与检索

- 公开示例食物库从 28 条扩充到 33 条，补齐披萨、三明治、热狗、甜甜圈和蛋糕，使可识别的 10 个类别都能走通检索、计算和保存。
- 同步重建向量索引与可移植评测报告；Recall@3 `0.9474` 和拒答准确率 `1.0` 与扩充前一致。

### 部署与配置

- 新增 `MEAL_DETECTION_MODE` 等配置项，默认 `disabled`，属于可稍后补充的准备项，缺失不会挡住任何核心功能。
- 新增 `scripts/prepare_food_detection_model.py`，按固定版本下载并导出本地推理权重。权重为 Ultralytics AGPL-3.0 授权，不随本仓库分发。
- 运行时只新增 `onnxruntime` 依赖；导出所需的 ultralytics 和 torch 不进入依赖清单，部署镜像大小和构建时间不受影响。
- CI 增加独立的真实权重冒烟作业，权重按固定版本缓存，主流程不受影响。

### 测试与验证

- 新增检测解码与非极大值抑制单测、多模态适配器单测、识别预填端到端流程，以及需要真实权重的冒烟测试。
- 保留并强化「关闭识别后仍能手动完整记一餐」的端到端断言，避免识别以后被做成前置依赖。
- 修复两处让浏览器测试不稳定的问题。其一，套件开始前增加一次只读预热往返：首次服务端往返实测约 1.9 秒、之后稳定在 130—150 毫秒，此前第一个带计时断言的用例会把冷启动误当成消息回显延迟。其二，打开页面后等待已保存对话恢复完成再操作：恢复会整体覆盖聊天区内容，在它落定之前发出的消息会被这次覆盖抹掉，用例随后在等一条已经不存在的消息；对话越长、机器越慢越容易触发。
- 验证结果：Python 测试 `292 passed`，浏览器测试 `14 passed`。

## 2026-09-13

### 多轮健康管理

- 将多轮能力从简单问答扩展为连续健康管理场景，覆盖一日饮食记录、体重平台期、运动恢复、睡眠改善、周期复盘、习惯建立、下一餐建议、安全分流、长期偏好和目标冲突。
- 支持跨轮补充缺失信息、自然语言修订待确认草稿、文本确认或取消，以及保存后继续查询和调整。
- 增加场景识别与上下文策略，明确切换主题时跟随新意图，省略式回答时延续当前健康任务。

### 健康知识与可观测性

- 健康知识检索升级为本地 Hybrid RAG：Embedding 稠密检索、词法检索、加权 RRF 融合、规则重排，以及 Top-K 和最低可信分数门控。
- 补充固定检索集、安全拒答集、Lexical 对比评测、索引构建脚本和可移植评测报告。
- 增加 LangSmith 合成往返验证脚本与操作文档；健康输入输出继续默认隐藏，可在明确授权的调试环境中临时显示后验证 Trace。

### 飞书提醒

- 提醒统一通过飞书发送，不再向用户提供本地提醒选项；历史本地提醒不再出现在用户提醒列表中，也不会继续发送。
- 支持“每天晚上 9 点提醒我拉伸”“工作日上午 10 点提醒我喝水”和“2 分钟后提醒我”等自然语言时间表达，不再要求用户填写程序化时间格式。
- 修改提醒时原位更新原记录，保留同一个提醒标识，避免修改前和修改后的提醒同时出现。
- 提醒列表由宽表格改为纵向卡片，长内容自动换行，修复滚动时标题、内容和操作区域重叠错位的问题。

### 对话与界面

- 食物候选、提醒和错误回答增加用户文案清理，不再暴露 `FOOD_002`、内部字段名和时间格式术语。
- 餐食图片确认页隐藏检索模式、内部数据集、耗时、分数和候选证据表，只保留用户需要的食物确认信息。
- 将“今日已确认记录”移到右侧信息栏，对话主区域只保留消息、处理状态、确认卡片和输入区。
- 修复提醒页点击当前“最近对话”无法返回对话界面的问题。
- 扩大用户消息气泡及其内部文字容器；短提醒句在桌面端保持单行，移动端仍可自适应且不会横向溢出。
- 将界面中的 `check-in`、Webhook 等实现词汇替换为“主动问候”“飞书连接”等普通用户可理解的表达。

### 测试与验证

- 新增周期提醒自然语言解析、提醒原位修改、内部食物编号清理和多轮健康管理场景回归测试。
- 新增最近对话导航、右侧今日记录、提醒卡片布局和消息气泡宽度的 Chromium 浏览器测试。
- 修复健康知识查询编码器缺失导致的静默降级：文档向量随仓库提交但编码器不提交，CI 与部署镜像中 Dense 召回都退回纯词法，语义改写问题返回 `KNOWLEDGE_NOT_FOUND`。
- 部署镜像在构建期预置编码器到 `/opt/huggingface` 并锁为离线，线上健康知识恢复 Hybrid 检索；此前线上「晚餐怎么搭配」这类问题答不出来。
- CI 按索引 manifest 固定值预取并缓存编码器，缺失时直接失败而不是降级。
- 新增 `scripts/prepare_health_knowledge_model.py`：按固定 revision 准备查询编码器并校验向量维度与已提交索引一致。
- `retrieval_receipt()` 的 `mode` 改为同时依赖索引和编码器，不再把纯词法检索报告成 `hybrid`；新增 `index_ready` 区分索引损坏与编码器缺失。
- 验证结果：Python 测试 `231 passed`，浏览器测试 `12 passed`；桌面端和 390px 移动端布局通过验收。

## 2026-09-11

### 界面调整

- 移除“记录会去哪里”状态卡及其状态提示、重置按钮和本地记忆说明，减少对话工作区中的重复信息。
- 保留底层会话状态、历史记录和确认机制；用户仍可在“对话”页面清除当前对话。
- 将餐食图片上传入口整合到对话输入区，不再保留独立的“餐食图片”页面和导航入口。
- 上传图片后在对话内展开食物名称、份量、可靠候选检索、营养估算及确认保存流程；沿用现有真实数据源和写入确认规则。
- 扩大桌面端对话区的宽度和高度，并增加消息气泡可用宽度；同步优化 390px 移动端消息区高度和图片流程布局。

### 修复

- 修复“我想创建一个提醒：两分钟后在飞书上提醒我去喝水”等复合自然语言句式无法识别的问题。
- 相对时间提醒现在直接读取会话时区的当前时间进行换算；`Asia/Shanghai` 用户无需手动提供 ISO 8601 时间。
- 支持识别“在飞书上”“通过飞书”等渠道表达，同时继续执行未来时间、提醒权限和飞书配置校验。

### 测试与验证

- 新增餐食图片从对话输入区上传、候选检索和生成待确认饮食记录的浏览器回归测试。
- 新增复合句式飞书提醒的时区、相对时间、提醒内容和渠道回归测试。
- 验证结果：Python 测试 `195 passed`，浏览器测试 `9 passed`，布局检测未发现错位问题，`git diff --check` 通过。

### 新增

- 新增多会话能力：支持创建新对话、查询历史会话、切换会话，并在历史会话中继续交流。
- 为内存与 SQLite 存储层补充会话摘要列表查询，列表标题从真实对话内容生成，不在正式页面中写入原型演示数据。
- 新增 AI 回答“处理过程”展示：请求进行中时呈现可理解的处理阶段，完成后可展开查看已执行步骤；不暴露模型内部推理文本。

### 界面重构

- 按高保真健康助理模型重构主页面布局、导航、顶部工具栏、今日观察、快捷记录、对话区、输入区与右侧摘要面板。
- 统一颜色、字体、间距、边框、阴影和响应式断点，并同步更新视觉设计规范。
- 历史会话改为单列独立滚动区域，修复长列表与底部信息互相覆盖、双列错位和侧栏内容溢出问题。
- 移除侧栏底部“本地真实数据 / 未同步至云端”提示。
- 桌面端保留三栏信息结构；390px 移动端改为适配窄屏的页面结构与交互，避免横向溢出。
- 现有后端接口、鉴权、路由、状态管理及健康记录业务流程保持不变。

### 修复

- 修复“提醒我 2 分钟后喝水”等前缀式相对时间提醒无法识别的问题。
- 提醒工具支持“2 分钟后”等相对时间值，并继续校验提醒时间必须晚于当前时间。
- 优化模型超时及工具调用失败时的错误反馈，避免失败状态与正常回复混淆。

### 测试与验证

- 补充会话摘要查询、SQLite 会话列表、多会话切换与续聊的单元测试和浏览器测试。
- 补充相对时间提醒的工具层与端到端回归测试。
- 浏览器测试覆盖 1920px、1440px 桌面端和 390px 移动端，以及会话持久化、处理过程展示和提醒创建流程。
- 验证结果：Python 测试 `194 passed`，浏览器测试 `8 passed`，布局检测未发现错位问题，`git diff --check` 通过。

### 说明

- 浏览器测试使用隔离的种子会话验证长列表和滚动行为；这些数据仅用于测试，不会进入正式运行数据。
- 暂未映射到新界面的旧业务能力没有删除，仍保留在原有实现中。
